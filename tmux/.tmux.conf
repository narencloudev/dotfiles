##### TERMINAL & COLOR #####
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",*256col*:Tc"

##### MOUSE #####
set -g mouse on

##### GENERAL #####
set -g set-clipboard on
set -g detach-on-destroy on
set -g escape-time 0
set -g history-limit 1000000
set -g status-interval 3
set -g allow-passthrough on
set -g default-terminal "${TERM}"

##### PREFIX #####
unbind C-b
set -g prefix C-Space
unbind r
bind r source-file ~/.tmux.conf \; display "Config Reloaded!"

##### WINDOWS / PANES #####
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on
set -g status-position top

##### PLUGINS #####
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'

# tmux-sessionx & fzf
set -g @plugin 'omerxx/tmux-sessionx'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @sessionx-zoxide-mode 'on'
set -g @sessionx-legacy-fzf-support 'on'
set -g @sessionx-bind-zo-new-window 'ctrl-y'
set -g @sessionx-auto-accept 'off'
set -g @sessionx-custom-paths '/Users/omerxx/dotfiles'
set -g @sessionx-bind 'o'
set -g @sessionx-x-path '~/dotfiles'
set -g @sessionx-window-height '85%'
set -g @sessionx-window-width '75%'
set -g @sessionx-zoxide-mode 'on'
set -g @sessionx-custom-paths-subdirectories 'false'
set -g @sessionx-filter-current 'false'


##### CONTINUUM & RESURRECT #####
set -g @continuum-restore 'off'
set -g @resurrect-capture-pane-contents 'on'



# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tmux-battery'



# Fallback color without Catppuccin
set -g @bg_color "default"   # Dark grey
set -g @fg_color "default"

# Configure Online Status
set -g @online_icon "ok"
set -g @offline_icon "nok"

# Status left
set -g status-left-length 100
set -g status-left ""
set -ga status-left "#[bg=#{@bg_color},fg=#{@fg_color}] #(whoami) "
set -ga status-left "#[bg=#{@bg_color},fg=#{@fg_color}]|"
set -ga status-left "#{?client_prefix,#[bg=#{@bg_color},#[bg=#{@bg_color},fg=#{@fg_color}]  #S }"

#set -ga status-left "#{?client_prefix,#[bg=#{@bg_color},fg=#{@fg_color},bold]  #S ,  #S }"
set -ga status-left "#[bg=#{@bg_color},fg=#{@fg_color}]|"
set -ga status-left "#[bg=#{@bg_color},fg=#{@fg_color}]  #{pane_current_command} "
set -ga status-left "#[bg=#{@bg_color},fg=#{@fg_color}]|"
set -ga status-left "#[bg=#{@bg_color},fg=#{@fg_color}]  #{=/-32/...:#{s|$USER|~|:#{b:pane_current_path}}} "
set -ga status-left "#{?window_zoomed_flag,│ zoom ,}"

# Status right
set -g status-right-length 100
set -g status-right ""
#set -ga status-right "#[bg=#{@bg_color},fg=#{@fg_color}] #{battery_icon} #{battery_percentage} "
#set -ga status-right "#[bg=#{@bg_color},fg=#{@fg_color}]│"
set -ga status-right "#(ps -o comm= -t $(tmux display -p '#{pane_tty}') | grep -q ssh && echo '🔑SSH:'$(ps -o args= -t $(tmux display -p '#{pane_tty}') | awk '{print $2}')) |"
set -ga status-right "#{?#{==:#{online_status},ok},#[fg=#{@fg_color}] 󰖩 on ,#[fg=#{@fg_color},bold,reverse] 󰖪 off }"
set -ga status-right "#[bg=#{@bg_color},fg=#{@fg_color}]|"
set -ga status-right "#[bg=#{@bg_color},fg=#{@fg_color}] 󰭦 %Y-%m-%d | 󰅐 %H:%M "


# Status bar position
set -g status-position top
set -g status-style "bg=#{@bg_color},fg=#{@fg_color}"
set -g status-justify "centre"

# Pane borders
setw -g pane-border-status top
setw -g pane-border-format ""
setw -g pane-active-border-style "bg=#{@bg_color},fg=#{@fg_color}"
setw -g pane-border-style "bg=#{@bg_color},fg=#{@fg_color}"
setw -g pane-border-lines single



# Window look and feel
set -wg automatic-rename on
set -g automatic-rename-format "Window"

set -g window-status-format " #I#{?#{!=:#{window_name},Window},:#W,} "
set -g window-status-style "bg=#{@bg_color},fg=#{@fg_color}"
set -g window-status-last-style "bg=#{@bg_color},fg=#{@fg_color}"
set -g window-status-activity-style "bg=#{@bg_color},fg=#{@fg_color}"
set -g window-status-bell-style "bg=#{@bg_color},fg=#{@fg_color},bold"
set -gF window-status-separator "#[bg=#{@bg_color},fg=#{@fg_color}]|"

set -g window-status-current-format " #I#{?#{!=:#{window_name},Window},:#W,} "
set -g window-status-current-style "bg=#{@bg_color},fg=#{@fg_color},bold"

# Restore default reverse-video selector highlight
set -g mode-style "reverse"

# Initialize TPM
run '~/.tmux/plugins/tpm/tpm'
